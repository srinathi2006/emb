#include<stdio.h>
int main()
{
    char a='a';
    if((a>=65)&&(a<=90))
    {
        printf("a is a uppercase alphabet");
    }
    else 
{
    printf("a is not an uppercase alphabet");
}
    return 0;
}